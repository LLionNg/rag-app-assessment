# Secrets only. Everything else is configured in config.yml.
# Copy to .env and fill in the provider you enabled under `llm.provider`.

# Bangkok Bank assessment gateway (gpt-5-mini via Azure API Management)
BBL_LLM_API_KEY=
